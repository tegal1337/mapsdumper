const { extractData } = require("./helper/extractData");
const { extractLinks } = require("./helper/extractLink");
const { waitForReviews } = require("./helper/waitForReviews");
module.exports = {
extractLinks,
waitForReviews,
extractData
}